#!/usr/bin/env python

import lbktools
import sys

def run():
    commands = {
        'run': {
            'mtag': lambda x: print "mtag: ",
            'cg3': lambda x: print "cg3: ",
            'stat': lambda x: print "stat: ",
        }
    }

    print sys.argv


if __name__ == '__main__':
    run()
